export const API_KEY = import.meta.env.VITE_GIPHY_API_KEY;
export const BASE_URI = import.meta.env.VITE_API_BASE_URI;

const config = {
  API_KEY,
  BASE_URI,
  VERSION: "1.0",
};

export default config;
